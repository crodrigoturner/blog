---
layout: layouts/base.njk
---

<span class="postnav"><a href="/">home</a> › <a href="/blog/">blog</a> ›
{%- for tag in tags | filterTagList %}
{%- set tagUrl %}/tags/{{ tag | slugify }}/{% endset %}
<a href="{{ tagUrl }}" class="post-tag">{{ tag }}</a>{%- if not loop.last %}, {% endif %}
{%- endfor %} ›
</span><h2> {{title}}</h2>

{{ content | safe }}

<!--
<span class="meta"><a href="mailto:rodrigoturner.carlos@gmail.com">Reply by email</a> · <time datetime="{{ page.date | htmlDateString }}">{{ page.date | readableDate("dd.MM.yyyy") }}</time></span>
-->
{%- for post in postslist | reverse %}
<a href="{{post.url}}">
<img src="{{post.data.cover}}" alt="cover image" class="cover"/> 
<span>
<h3>{{ post.data.title }} </h3> 
<p class="excerpt">{{ post.data.excerpt | safe }}</p>
</span>
</a>
{%- endfor %}